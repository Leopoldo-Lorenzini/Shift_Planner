<h1>Mis Viajes</h1>

<h2>Como Piloto</h2>
<% if @pilot_trips.any? %>
  <ul>
    <% @pilot_trips.each do |trip| %>
      <li>
        <strong>Inicio:</strong> <%= trip.starting_place.name %>
        <strong>Destino:</strong> <%= trip.ending_place.name %>
        <strong>Hora de inicio:</strong> <%= trip.h_start %>
        <strong>Hora de fin:</strong> <%= trip.h_end %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No tienes viajes como piloto.</p>
<% end %>

<h2>Como Pasajero</h2>
<% if @passenger_trips.any? %>
  <ul>
    <% @passenger_trips.each do |trip| %>
      <li>
        <strong>Inicio:</strong> <%= trip.starting_place.name %>
        <strong>Destino:</strong> <%= trip.ending_place.name %>
        <strong>Hora de inicio:</strong> <%= trip.h_start %>
        <strong>Hora de fin:</strong> <%= trip.h_end %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No tienes viajes como pasajero.</p>
<% end %>

<%= render 'shared/navbar' %>

<div class="container mt-4">
  <%= yield %>
</div>